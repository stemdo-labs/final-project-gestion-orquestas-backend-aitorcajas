name: Buildear imagen del backend

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: final-project-exercise-aitorcajas/.github/workflows/image-build.yml@main
    with:
      repo: ${{ github.repository }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}